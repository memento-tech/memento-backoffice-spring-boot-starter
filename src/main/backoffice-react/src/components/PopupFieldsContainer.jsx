import styled from "styled-components";

const PopupFieldsContainer = styled.div`
  padding-top: 2rem;
  padding-left: 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  width: 950px;
`;

export default PopupFieldsContainer;
